#SpringBoot整合Mybatis相关配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/exadata?characterEncoding=utf-8&serverTimezone=UTC
    username: peter
    password: peter
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml # mapper映射文件路径
  type-aliases-package: com.hzau.Domain # 配置哪个包下的类有默认的别名